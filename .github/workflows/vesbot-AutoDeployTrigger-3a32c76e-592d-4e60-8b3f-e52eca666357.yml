name: Trigger auto deployment for vesbot

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/vesbot-AutoDeployTrigger-3a32c76e-592d-4e60-8b3f-e52eca666357.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.VESBOT_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.VESBOT_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.VESBOT_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: 
          registryUsername: ${{ secrets.VESBOT_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.VESBOT_REGISTRY_PASSWORD }}
          containerAppName: vesbot
          resourceGroup: DefaultResourceGroup-SUK
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


